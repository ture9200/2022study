const x = 0;
const y = 0;
const coordinate = { x: x, y: y }; // {x,y};
console.log(coordinate); // {X:0 , y:0}

function makeObj(name, age) {
  return {
    name: name, // name
    age: age, //age
  };
}
